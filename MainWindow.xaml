<Window x:Class="CacheLoginToolWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CacheLoginToolWPF"
        Title="Nice Alts CS2 Account Manager" 
        Height="700" Width="1400"
        Background="#000000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="240"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="32,32,32,32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Accounts" 
                       FontSize="32" 
                       FontWeight="Light"
                       Foreground="#FFFFFF"
                       Margin="0,0,0,24"
                       FontFamily="{StaticResource PoppinsFont}"/>

            <Border Grid.Row="1"
                    Background="#000000"
                    BorderBrush="#1A1A1A"
                    BorderThickness="1"
                    CornerRadius="0">
                    <DataGrid x:Name="AccountsDataGrid"
                              MouseDoubleClick="AccountsDataGrid_MouseDoubleClick"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          Foreground="#FFFFFF"
                          BorderThickness="0"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Extended"
                          SelectionUnit="FullRow"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          FontSize="13"
                          FontFamily="{StaticResource PoppinsFont}"
                          RowHeight="44"
                          AllowDrop="True"
                          Drop="AccountsDataGrid_Drop"
                          DragOver="AccountsDataGrid_DragOver">
                    <DataGrid.InputBindings>
                        <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DoubleClickCommand}"/>
                    </DataGrid.InputBindings>
                    <DataGrid.Columns>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="250" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Steam ID" Binding="{Binding SteamId}" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#1A1A1A"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#0A0A0A"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="#FFFFFF"/>
                </DataGrid.Resources>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#808080"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#1A1A1A"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="Normal"/>
                        <Setter Property="FontFamily" Value="{StaticResource PoppinsFont}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#0A0A0A"/>
                        <Setter Property="MinHeight" Value="44"/>
                        <Setter Property="Height" Value="44"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0A0A0A"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="16,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}"
                                            VerticalAlignment="Stretch">
                                        <ContentPresenter VerticalAlignment="Center" 
                                                        HorizontalAlignment="Left"
                                                        Margin="0"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.ContextMenu>
                    <ContextMenu Background="#1A1A1A" 
                                  Foreground="#FFFFFF"
                                  BorderBrush="#1A1A1A"
                                  BorderThickness="1">
                        <MenuItem Header="Login with account" 
                                  Click="LoginMenuItem_Click"
                                  FontFamily="{StaticResource PoppinsFont}">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="#1A1A1A"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                        <MenuItem Header="View in browser" 
                                  Click="ViewBrowserMenuItem_Click"
                                  FontFamily="{StaticResource PoppinsFont}">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="#1A1A1A"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                        <Separator Background="#1A1A1A"/>
                        <MenuItem Header="Delete Selected Rows" 
                                  Click="DeleteMenuItem_Click"
                                  FontFamily="{StaticResource PoppinsFont}">
                            <MenuItem.Style>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Background" Value="#1A1A1A"/>
                                    <Setter Property="Foreground" Value="#FFFFFF"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </MenuItem.Style>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                </DataGrid>
            </Border>
        </Grid>

        <Border Grid.Column="1" 
                Background="#000000" 
                BorderBrush="#1A1A1A" 
                BorderThickness="1,0,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20,28,20,28">

                    <TextBlock Text="ACTIONS" 
                               FontSize="11" 
                               FontWeight="Normal"
                               Foreground="#808080"
                               Margin="0,0,0,12"
                               FontFamily="{StaticResource PoppinsFont}"/>

                    <Button x:Name="StartButton" 
                            Content="Start" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="StartButton_Click"
                            Margin="0,0,0,8"
                            Height="36"/>

                    <Button x:Name="LoginButton" 
                            Content="Login" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="LoginButton_Click"
                            Margin="0,0,0,8"
                            Height="36"/>

                    <Button x:Name="LoadButton" 
                            Content="Import Accounts" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="LoadButton_Click"
                            Margin="0,0,0,8"
                            Height="36"/>

                    <Button x:Name="ClearButton" 
                            Content="Clear" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="ClearButton_Click"
                            Margin="0,0,0,24"
                            Height="36"/>

                    <TextBlock Text="STATUS" 
                               FontSize="11" 
                               FontWeight="Normal"
                               Foreground="#808080"
                               Margin="0,0,0,12"
                               FontFamily="{StaticResource PoppinsFont}"/>

                    <Border Background="#1A1A1A"
                            CornerRadius="0"
                            Padding="12"
                            Margin="0,0,0,24"
                            BorderThickness="1"
                            BorderBrush="#1A1A1A">
                        <TextBlock x:Name="StatusLabel" 
                                   Text="Not Logged in." 
                                   FontSize="13" 
                                   FontWeight="Normal"
                                   Foreground="#FFFFFF"
                                   TextWrapping="Wrap"
                                   FontFamily="{StaticResource PoppinsFont}"/>
                    </Border>

                    <TextBlock Text="ADDITIONAL" 
                               FontSize="11" 
                               FontWeight="Normal"
                               Foreground="#808080"
                               Margin="0,0,0,12"
                               FontFamily="{StaticResource PoppinsFont}"/>

                    <Button x:Name="KillSteamButton" 
                            Content="Kill Steam" 
                            Style="{StaticResource DestructiveButtonStyle}"
                            Click="KillSteamButton_Click"
                            Margin="0,0,0,8"
                            Height="36"/>

                    <Button x:Name="ExportButton" 
                            Content="Export accounts..." 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="ExportButton_Click"
                            Margin="0,0,0,8"
                            Height="36"/>

                    <TextBlock Text="API" 
                               FontSize="11" 
                               FontWeight="Normal"
                               Foreground="#808080"
                               Margin="0,0,0,12"
                               FontFamily="{StaticResource PoppinsFont}"/>

                    <Button x:Name="GenerateAccountButton" 
                            Content="Generate" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="GenerateAccountButton_Click"
                            Margin="0,0,0,8"
                            Height="36"/>

                    <Button x:Name="SetApiKeyButton" 
                            Content="Set API Key" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="SetApiKeyButton_Click"
                            Height="36"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>

